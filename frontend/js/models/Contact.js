/** @format */

export default class Contact {
  constructor(firstName, lastName, email, address, zip, city) {
    this.firstName = firstName,
    this.lastName = lastName,
    this.email = email,
    this.address = address,
    this.zip = zip,
    this.city = city;
  }
}
